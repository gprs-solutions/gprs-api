version: "3.8"

services:
  api:
    container_name: gprs.api
    build:
      context: .
      dockerfile: Dockerfile
    volumes:
      - .:/var/www/html
    ports:
      - "8030:8000"
    command: php artisan serve --host=0.0.0.0 --port=8000
