name: 🔍 List Backups

on:
  workflow_dispatch:

jobs:
  list-backups:
   runs-on: ubuntu-latest
   steps:
    - name: 📂 List Database Backups
      run: |
        # Connect to the server and list all files in the 'backups' directory
        ssh -p 2222 ${{ secrets.SSH_USER }} "ls -a /backups" 